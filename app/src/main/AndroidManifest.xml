<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="app.pavel.handbooklivedataroom">

    <application
        android:allowBackup="true"
        android:name=".utils.HandbookLiveDataRoom"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".ui.LaunchActivity" android:theme="@style/noAnimTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.SelectedRuleActivity" android:theme="@style/noAnimTheme"/>

        <activity android:name=".ui.TrafficRulesActivity" android:theme="@style/noAnimTheme"/>

        <activity android:name=".ui.TrafficSignsActivity" android:theme="@style/noAnimTheme"/>

        <activity android:name=".ui.SelectedSignActivity" android:theme="@style/noAnimTheme"/>

        <activity android:name=".ui.RoadMarkingActivity" android:theme="@style/noAnimTheme"/>
    </application>

</manifest>